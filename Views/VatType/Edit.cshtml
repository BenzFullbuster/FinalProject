@model FinalProject.Models.ViewModels.VatTypeEditViewModel

@{
    ViewData["Title"] = "VatType";
}

<div>
    <h1 class="text-primary mb-0">แก้ไขประเภทภาษี</h1>
    <hr />
</div>

<div>
    <form asp-action="Edit" asp-route-id="@Model.VatTypeId" method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input asp-for="VatTypeId" type="hidden" />
        <input asp-for="RowVersion" type="hidden" />
        <div class="mb-3 was-validated">
            <label asp-for="VatTypeName" class="form-label"></label>
            <input asp-for="VatTypeName" type="text" class="form-control" required>
            <span asp-validation-for="VatTypeName" class="text-danger"></span>
        </div>
        <div>
            <label asp-for="Description" class="form-label"></label>
            <textarea asp-for="Description" type="text" class="form-control"></textarea>
            <span asp-validation-for="Description" class="text-danger"></span>
        </div>
        <br />
        <div>
            <button type="submit" class="btn btn-outline-success">
                <i class="bi bi-save"></i> บันทึก
            </button>
            <a asp-action="Index" class="btn btn-outline-secondary">
                <i class="bi bi-x-square"></i> ยกเลิก
            </a>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}